#pragma once
#include <LocalLLM.h>

/**
 * @brief Gets the directory path where the current executable is located.
 * @return A wide string containing the directory path of the current executable, without a trailing slash.
 */
QFont makeCodeFont(int pt);